{
  "name": "portfolio-monorepo",
  "private": true,
  "workspaces": [
    "Frontend",
    "portfolio-backend"
  ],
  "scripts": {
    "dev": "concurrently -k -n frontend,backend -c blue,green \"npm run dev --workspace frontend\" \"npm run dev --workspace bigjonesportfolio-backend\"",
    "start": "concurrently -k -n frontend,backend -c blue,green \"npm run start --workspace frontend\" \"npm run start --workspace bigjonesportfolio-backend\"",
    "build": "npm run build --workspace frontend",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm test --workspace bigjonesportfolio-backend",
    "check": "npm run lint && npm test && npm run build"
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  },
  "engines": {
    "node": "22.x"
  }
}
